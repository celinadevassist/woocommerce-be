export enum FieldType {
  TEXT = 'text',
  IMAGE_SWATCH = 'image_swatch',
}

export enum FieldsetStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum AssignmentType {
  PRODUCT = 'product',
  CATEGORY = 'category',
  ALL = 'all',
}
